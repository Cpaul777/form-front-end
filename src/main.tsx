import React from 'react'
import ReactDOM from 'react-dom/client'
import TripTicketForm from './tripTicketForm.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TripTicketForm />
  </React.StrictMode>,
)